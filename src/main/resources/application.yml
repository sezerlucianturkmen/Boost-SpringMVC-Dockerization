server:
  port: 9091

spring:
  datasource:
    drive-clas-name: org.postgresql.Driver
    url: jdbc:postgresql://webapp-postgresql/WebAppDB
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true